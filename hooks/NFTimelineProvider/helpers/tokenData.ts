/** @format */

import { SingleNFTDataType } from "e-labs_web3provider";

export const getTokenMetadata = async (
  network: string,
  contractAddress: string,
  tokenId: string
): Promise<SingleNFTDataType> => {
  let tokenData: SingleNFTDataType;
  return tokenData;
};
